# Public deployment configuration. Copy to .env.local for local overrides.
# These values are not secrets: they appear in the generated site's URLs.
SITE_URL=https://lucafregoso.github.io
BASE_PATH=/codewhisperer/
